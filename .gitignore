# macOS
.DS_Store

# stray local screenshot dir, not part of the site
Home/

# local dev tooling: launch.json points at an absolute /tmp path
.claude/
# the self-contained build: 7MB of inlined assets, rebuilt on demand rather
# than tracked — it would more than double the repo on its own
drumwave-standalone.html
.build-cache/
